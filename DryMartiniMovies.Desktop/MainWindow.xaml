<ui:FluentWindow x:Class="DryMartiniMovies.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DryMartiniMovies.Desktop"
        mc:Ignorable="d"
        Title="Dry Martini Movies" Height="600" Width="1200">
    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </ui:FluentWindow.Resources>
    <Grid x:Name="RootGrid" Background="#0d0d1a">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0"/>
        <TextBlock Grid.Row="1" Text="Hitta kopplingar mellan filmer 🍸" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10,0,10,16"/>
        <!-- Inputs and button row -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10,0,10,0">
            
            <Grid Margin="0,0,10,0">
                <TextBox x:Name="TextBox1" Width="200" Padding="0,0,30,0" Height="30" FontSize="16" Text="{Binding Movie1, UpdateSourceTrigger=PropertyChanged}"/>
                <Popup IsOpen="{Binding ShowHistory1}" PlacementTarget="{Binding ElementName=TextBox1}" Placement="Bottom">
                    <Border MaxHeight="300" Background="#2a2a3e" >
                    <ScrollViewer>
                <ItemsControl ItemsSource="{Binding UserHistory1}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Movie.Title}" Command="{Binding DataContext.SaveTmdbId1Command, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                    </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>

            <Grid Margin="0,0,10,0">
                <TextBox x:Name="TextBox2" Width="200" Padding="0,0,30,0" Height="30" FontSize="16" Text="{Binding Movie2, UpdateSourceTrigger=PropertyChanged}"/>
                <Popup IsOpen="{Binding ShowHistory2}" PlacementTarget="{Binding ElementName=TextBox2}" Placement="Bottom">
                    <Border MaxHeight="300" Background="#2a2a3e" >
                    <ScrollViewer>
                <ItemsControl ItemsSource="{Binding UserHistory2}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Movie.Title}" Command="{Binding DataContext.SaveTmdbId2Command, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>

            <ui:Button Content="Hitta koppling" Command="{Binding FindPathCommand}" VerticalAlignment="Top" Height="30" CornerRadius="1"/>
        </StackPanel>

        <!-- Result area -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" Margin="10,0,10,10">
            <TextBlock Text="{Binding ErrorMessage}" Margin="0,10,0,0"/>
            <ItemsControl ItemsSource="{Binding MoviePathDto.Steps}" Margin="0,10,0,0" AlternationCount="1000">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text=" → " VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock VerticalAlignment="Center">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1})">
                                        <Binding Path="Name" />
                                        <Binding Path="Type" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
