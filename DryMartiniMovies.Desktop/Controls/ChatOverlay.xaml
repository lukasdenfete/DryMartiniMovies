<UserControl x:Class="DryMartiniMovies.Desktop.Controls.ChatOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             HorizontalAlignment="Right"
             VerticalAlignment="Bottom"
             Margin="0,0,20,20">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Scroll to bottom when items change -->
        <Style x:Key="AutoScrollListBox" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <!-- Toggle button style (robot bubble) -->
        <Style x:Key="BubbleButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon button style (send / clear) -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="18"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1a4a7a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel HorizontalAlignment="Right">

        <!-- Clear button (only when open) -->
        <Button x:Name="ClearButton"
                Style="{StaticResource IconButton}"
                Width="32" Height="32"
                Background="#1e1e33"
                BorderBrush="#555"
                Margin="0,0,0,6"
                ToolTip="Rensa chatt"
                Click="ClearButton_Click"
                Visibility="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="↺" FontSize="16" Foreground="#aaa" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Button>

        <!-- Chat window (only when open) -->
        <Border x:Name="ChatWindow"
                Width="300" Height="400"
                Background="#1a1a2e"
                BorderBrush="#555"
                BorderThickness="1"
                CornerRadius="10"
                Margin="0,0,0,8"
                Visibility="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Messages -->
                <ListBox x:Name="MessageList"
                         Grid.Row="0"
                         Style="{StaticResource AutoScrollListBox}"
                         ItemsSource="{Binding Messages, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         Margin="5,5,5,0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <ContentControl x:Name="MessageContainer" Margin="4,3">
                                <Border x:Name="Bubble"
                                        CornerRadius="10"
                                        Padding="8,5"
                                        MaxWidth="220"
                                        HorizontalAlignment="Right"
                                        Background="#16213e"
                                        BorderBrush="Black"
                                        BorderThickness="1">
                                    <TextBlock Text="{Binding Content}"
                                               Foreground="White"
                                               TextWrapping="Wrap"
                                               FontFamily="Segoe UI"
                                               FontSize="13"/>
                                </Border>
                            </ContentControl>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Role}" Value="assistant">
                                    <Setter TargetName="Bubble" Property="Background" Value="#0f3460"/>
                                    <Setter TargetName="Bubble" Property="HorizontalAlignment" Value="Left"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Input row -->
                <Grid Grid.Row="1" Margin="8,6,8,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="InputBox"
                             Grid.Column="0"
                             Background="#16213e"
                             Foreground="White"
                             CaretBrush="White"
                             BorderBrush="#0f3460"
                             BorderThickness="1"
                             Padding="8,6"
                             FontSize="13"
                             VerticalContentAlignment="Center"
                             Text="{Binding InputText, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="10">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <Button x:Name="SendButton"
                            Grid.Column="1"
                            Style="{StaticResource IconButton}"
                            Width="36" Height="36"
                            Background="#0f3460"
                            Margin="6,0,0,0"
                            ToolTip="Skicka"
                            Click="SendButton_Click">
                        <TextBlock Text="➤" FontSize="14" Foreground="White"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Tooltip (only when closed) -->
        <Border x:Name="TooltipBubble"
                Background="#1a1a2e"
                BorderBrush="#444"
                BorderThickness="1"
                CornerRadius="12"
                Padding="8,4"
                Margin="0,0,0,4"
                HorizontalAlignment="Right"
                Visibility="{Binding IsClosedTooltipVisible, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Fråga mig!" Foreground="#eee" FontSize="12" FontFamily="Segoe UI"/>
        </Border>

        <!-- Robot toggle button -->
        <Button Style="{StaticResource BubbleButton}"
                HorizontalAlignment="Right"
                Click="ToggleButton_Click">
            <TextBlock Text="🤖" FontSize="42" ToolTip="Öppna/stäng chatt"/>
        </Button>

    </StackPanel>
</UserControl>
